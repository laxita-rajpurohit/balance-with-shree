# Balance with Shree Workspace

The Vite app lives in `./balance-with-shree`.

You can run the project from either location:

- repo root: `npm run dev`
- app folder: `cd balance-with-shree && npm run dev`

Both commands serve the same app code.
