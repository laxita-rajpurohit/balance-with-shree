{
  "name": "balance-with-shree-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix balance-with-shree run dev --",
    "build": "npm --prefix balance-with-shree run build",
    "lint": "npm --prefix balance-with-shree run lint",
    "preview": "npm --prefix balance-with-shree run preview --"
  }
}
